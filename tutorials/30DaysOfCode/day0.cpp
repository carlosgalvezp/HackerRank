#include <iostream>
#include <string>

int main()
{
    // Read input
    std::string text;
    std::getline(std::cin, text);

    // Print output
    std::cout << "Hello, World." << std::endl << text;
    return 0;
}
